// enum recovered from hlboot.dat via crashlink; param types are Dynamic placeholders.
enum LootType {
	MoneyGold(p0:Dynamic);
	MoneyGem(p0:Dynamic);
	GenericCell;
	GenericCellBonus;
	Blueprint(p0:Dynamic);
	InvItem(p0:Dynamic);
	Ammo(p0:Dynamic, p1:Dynamic);
	LeechDrop;
	HealDrop(p0:Dynamic);
	MoneyGoldCombo(p0:Dynamic);
	MoneyGemCombo(p0:Dynamic);
	Heart(p0:Dynamic);
}
