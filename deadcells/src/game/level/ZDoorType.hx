package level;

// enum recovered from hlboot.dat via crashlink.
enum ZDoorType {
	BossRune(arg0:Int);
	PerfectKills(arg0:Int);
	Timed(arg0:Float);
	Conditional;
	TumulusAntichamber;
	CliffEnigma;
	TrainingArena;
	PurpleTeleport;
	BossRushTeleport;
}
