package tool;

// enum recovered from hlboot.dat via crashlink; param types are Dynamic placeholders.
enum InventItemKind {
	Weapon(p0:Dynamic);
	Active(p0:Dynamic);
	Talisman(p0:Dynamic);
	BagItem(p0:Dynamic);
	__D_Passive;
	Consumable(p0:Dynamic);
	PreciousLoot(p0:Dynamic);
	Meta(p0:Dynamic);
	Perk(p0:Dynamic);
	Skin(p0:Dynamic);
	Head(p0:Dynamic);
	Aspect(p0:Dynamic);
	BossRushStatueUnlock(p0:Dynamic);
}
